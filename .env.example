# JobHunter 本地环境变量模板。
#
# 用法：复制成 .env 再填入真实取值。.env 已列入 .gitignore，不会被提交。
#
#   cp .env.example .env
#
# 为什么需要这个文件：launchd 启动的定时任务不读取 shell profile，
# 你写在 ~/.zshrc 里的 export 对它无效。手动在终端跑 run.py 不受影响。
#
# 只需要填 config.yaml 里 llm.provider 对应的那一个。

DEEPSEEK_API_KEY=
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
